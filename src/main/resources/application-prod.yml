spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/safetwin

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

logging:
  level:
    root: WARN
    com.safetwin: INFO
